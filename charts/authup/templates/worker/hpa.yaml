{{- if and .Values.server.enabled .Values.worker.enabled .Values.worker.autoscaling.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "authup.worker.fullname" . }}
  namespace: {{ include "authup.namespace" . | quote }}
  labels: {{- include "authup.labels" (dict "context" $ "component" "worker") | nindent 4 }}
  annotations: {{- include "authup.annotations" (dict "context" $) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "authup.worker.fullname" . }}
  minReplicas: {{ .Values.worker.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.worker.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- if .Values.worker.autoscaling.hpa.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.worker.autoscaling.hpa.targetCPU }}
    {{- end }}
    {{- if .Values.worker.autoscaling.hpa.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.worker.autoscaling.hpa.targetMemory }}
    {{- end }}
{{- end }}
