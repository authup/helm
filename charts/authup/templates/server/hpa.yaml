{{- if and .Values.server.enabled .Values.server.autoscaling.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "authup.server.fullname" . }}
  namespace: {{ include "authup.namespace" . | quote }}
  labels: {{- include "authup.labels" (dict "context" $ "component" "server") | nindent 4 }}
  annotations: {{- include "authup.annotations" (dict "context" $) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "authup.server.fullname" . }}
  minReplicas: {{ .Values.server.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.server.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- if .Values.server.autoscaling.hpa.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.server.autoscaling.hpa.targetCPU }}
    {{- end }}
    {{- if .Values.server.autoscaling.hpa.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.server.autoscaling.hpa.targetMemory }}
    {{- end }}
{{- end }}
