{{- if and .Values.server.enabled .Values.server.migration.enabled }}
{{/*
Pre-upgrade-only hook: on fresh installs the database backing service does not
exist before hooks run (hooks precede regular resources), and server-core
migrates at boot anyway. On upgrades the Job serializes DDL before new pods
roll, which matters for multi-replica deployments (MySQL DDL is not
transactional). The env ConfigMap content is INLINED so the hook never runs
against the previous release's ConfigMap.

ArgoCD has no pre-upgrade concept, so useHelmHooks=false hits the same
fresh-install problem a different way: a PreSync hook runs before every
Sync-phase resource, including the built-in database, so the Job used to fail
against a database that could never appear. The fix keeps the Job a hook (for
BeforeHookCreation delete-and-recreate on every sync) but moves it into the
Sync phase at sync-wave -1, ordered by wave instead of by phase. Everything
the Job's pod spec can reference (the built-in database, the ServiceAccount,
and the auth/external-db Secrets when they carry the values it needs) renders
at wave -10; the hook-scoped ConfigMap and NetworkPolicy at -5; so the Job
always runs after all of its own inputs exist and are healthy, and still
before the server Deployment (implicit wave 0).
*/}}
apiVersion: batch/v1
kind: Job
metadata:
  {{- /* Built from the component helper with the WHOLE "server-migration"
         suffix rather than by appending to authup.server.fullname, so the
         63-character budget is derived once instead of truncating a name that
         was already assembled. It matters here because a Job name is copied
         into the job-name / batch.kubernetes.io/job-name pod labels with no
         hand-written spec.selector, and a label VALUE stops at 63: appending
         to the -server name reaches 69 and the hook becomes unschedulable,
         hanging the upgrade. Identical output for any normal release name. */}}
  name: {{ include "authup.component.fullname" (dict "context" . "suffix" "server-migration") }}
  namespace: {{ include "authup.namespace" . | quote }}
  labels: {{- include "authup.labels" (dict "context" $ "component" "migration") | nindent 4 }}
  annotations:
    {{- include "authup.annotations" (dict "context" $) | nindent 4 }}
    {{- if .Values.useHelmHooks }}
    helm.sh/hook: pre-upgrade
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
    helm.sh/hook-weight: "0"
    {{- else }}
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
    argocd.argoproj.io/sync-wave: "-1"
    {{- end }}
spec:
  backoffLimit: {{ .Values.server.migration.backoffLimit }}
  {{- if .Values.server.migration.ttlSecondsAfterFinished }}
  ttlSecondsAfterFinished: {{ .Values.server.migration.ttlSecondsAfterFinished }}
  {{- end }}
  template:
    metadata:
      labels: {{- include "authup.labels" (dict "context" $ "component" "migration") | nindent 8 }}
      {{- if .Values.server.migration.podAnnotations }}
      annotations: {{- include "authup.tplvalues.render" (dict "value" .Values.server.migration.podAnnotations "context" $) | nindent 8 }}
      {{- end }}
    spec:
      restartPolicy: Never
      serviceAccountName: {{ include "authup.serviceAccountName" . }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
      {{- include "authup.imagePullSecrets" . | nindent 6 }}
      {{- if .Values.server.podSecurityContext.enabled }}
      securityContext: {{- include "authup.securityContext" .Values.server.podSecurityContext | nindent 8 }}
      {{- end }}
      containers:
        - name: migration
          image: {{ include "authup.appImage" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if .Values.server.containerSecurityContext.enabled }}
          securityContext: {{- include "authup.securityContext" .Values.server.containerSecurityContext | nindent 12 }}
          {{- end }}
          args:
            - migration
            - run
          env:
            {{- range $key, $value := (include "authup.server.configEnv" . | fromYaml) }}
            {{- if ne $key "PROVISIONING_DIRECTORY_PATH" }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- end }}
            {{- include "authup.server.secretEnv" (dict "context" $ "role" "migration") | nindent 12 }}
          {{- $resources := .Values.server.migration.resources | default .Values.server.resources }}
          {{- if $resources }}
          resources: {{- toYaml $resources | nindent 12 }}
          {{- end }}
          volumeMounts: {{- include "authup.server.volumeMounts" (dict "context" $ "role" "migration") | nindent 12 }}
      volumes: {{- include "authup.server.volumes" (dict "context" $ "role" "migration") | nindent 8 }}
{{- end }}
