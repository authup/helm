{{- if and .Values.server.enabled .Values.server.splitConsoles .Values.authConsole.enabled .Values.authConsole.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "authup.authConsole.fullname" . }}
  namespace: {{ include "authup.namespace" . | quote }}
  labels: {{- include "authup.labels" (dict "context" $ "component" "auth-console") | nindent 4 }}
  annotations: {{- include "authup.annotations" (dict "context" $) | nindent 4 }}
spec:
  podSelector:
    matchLabels: {{- include "authup.matchLabels" (dict "context" $ "component" "auth-console") | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - ports:
        - port: {{ .Values.authConsole.containerPorts.http }}
          protocol: TCP
      {{- if not .Values.authConsole.networkPolicy.allowExternal }}
      from:
        - podSelector: {}
        {{- if .Values.authConsole.networkPolicy.ingressPodMatchLabels }}
        - podSelector:
            matchLabels: {{- include "authup.tplvalues.render" (dict "value" .Values.authConsole.networkPolicy.ingressPodMatchLabels "context" $) | nindent 14 }}
        {{- end }}
        {{- if .Values.authConsole.networkPolicy.ingressNSMatchLabels }}
        - namespaceSelector:
            matchLabels: {{- include "authup.tplvalues.render" (dict "value" .Values.authConsole.networkPolicy.ingressNSMatchLabels "context" $) | nindent 14 }}
        {{- end }}
      {{- end }}
    {{- if .Values.authConsole.networkPolicy.extraIngress }}
    {{- include "authup.tplvalues.render" (dict "value" .Values.authConsole.networkPolicy.extraIngress "context" $) | nindent 4 }}
    {{- end }}
  egress:
    {{- if .Values.authConsole.networkPolicy.allowExternalEgress }}
    - {}
    {{- else }}
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    - to:
        - podSelector:
            matchLabels: {{- include "authup.matchLabels" (dict "context" $ "component" "server") | nindent 14 }}
    {{- end }}
    {{- if .Values.authConsole.networkPolicy.extraEgress }}
    {{- include "authup.tplvalues.render" (dict "value" .Values.authConsole.networkPolicy.extraEgress "context" $) | nindent 4 }}
    {{- end }}
{{- end }}
