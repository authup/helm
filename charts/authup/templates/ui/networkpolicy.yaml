{{- if and .Values.ui.enabled .Values.ui.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "authup.ui.fullname" . }}
  namespace: {{ include "authup.namespace" . | quote }}
  labels: {{- include "authup.labels" (dict "context" $ "component" "ui") | nindent 4 }}
  annotations: {{- include "authup.annotations" (dict "context" $) | nindent 4 }}
spec:
  podSelector:
    matchLabels: {{- include "authup.matchLabels" (dict "context" $ "component" "ui") | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - ports:
        - port: 3000
          protocol: TCP
      {{- if not .Values.ui.networkPolicy.allowExternal }}
      {{- /* An empty `from:` would mean allow-all — always emit at least the
             same-namespace baseline peer. */}}
      from:
        - podSelector: {}
        {{- if .Values.ui.networkPolicy.ingressPodMatchLabels }}
        - podSelector:
            matchLabels: {{- include "authup.tplvalues.render" (dict "value" .Values.ui.networkPolicy.ingressPodMatchLabels "context" $) | nindent 14 }}
        {{- end }}
        {{- if .Values.ui.networkPolicy.ingressNSMatchLabels }}
        - namespaceSelector:
            matchLabels: {{- include "authup.tplvalues.render" (dict "value" .Values.ui.networkPolicy.ingressNSMatchLabels "context" $) | nindent 14 }}
        {{- end }}
      {{- end }}
    {{- if .Values.ui.networkPolicy.extraIngress }}
    {{- include "authup.tplvalues.render" (dict "value" .Values.ui.networkPolicy.extraIngress "context" $) | nindent 4 }}
    {{- end }}
  egress:
    {{- if .Values.ui.networkPolicy.allowExternalEgress }}
    - {}
    {{- else }}
    # DNS
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    # server-core within the release
    - to:
        - podSelector:
            matchLabels: {{- include "authup.matchLabels" (dict "context" $ "component" "server") | nindent 14 }}
    {{- end }}
    {{- if .Values.ui.networkPolicy.extraEgress }}
    {{- include "authup.tplvalues.render" (dict "value" .Values.ui.networkPolicy.extraEgress "context" $) | nindent 4 }}
    {{- end }}
{{- end }}
