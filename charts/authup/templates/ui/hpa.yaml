{{- if and .Values.ui.enabled .Values.ui.autoscaling.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "authup.ui.fullname" . }}
  namespace: {{ include "authup.namespace" . | quote }}
  labels: {{- include "authup.labels" (dict "context" $ "component" "ui") | nindent 4 }}
  annotations: {{- include "authup.annotations" (dict "context" $) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "authup.ui.fullname" . }}
  minReplicas: {{ .Values.ui.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.ui.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- if .Values.ui.autoscaling.hpa.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.ui.autoscaling.hpa.targetCPU }}
    {{- end }}
    {{- if .Values.ui.autoscaling.hpa.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.ui.autoscaling.hpa.targetMemory }}
    {{- end }}
{{- end }}
