{{- if and .Values.adminConsole.enabled .Values.adminConsole.autoscaling.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "authup.adminConsole.fullname" . }}
  namespace: {{ include "authup.namespace" . | quote }}
  labels: {{- include "authup.labels" (dict "context" $ "component" "admin-console") | nindent 4 }}
  annotations: {{- include "authup.annotations" (dict "context" $) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "authup.adminConsole.fullname" . }}
  minReplicas: {{ .Values.adminConsole.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.adminConsole.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- if .Values.adminConsole.autoscaling.hpa.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.adminConsole.autoscaling.hpa.targetCPU }}
    {{- end }}
    {{- if .Values.adminConsole.autoscaling.hpa.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.adminConsole.autoscaling.hpa.targetMemory }}
    {{- end }}
{{- end }}
