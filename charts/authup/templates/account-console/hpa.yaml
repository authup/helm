{{- if and .Values.server.enabled .Values.server.splitConsoles .Values.accountConsole.enabled .Values.accountConsole.autoscaling.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "authup.accountConsole.fullname" . }}
  namespace: {{ include "authup.namespace" . | quote }}
  labels: {{- include "authup.labels" (dict "context" $ "component" "account-console") | nindent 4 }}
  annotations: {{- include "authup.annotations" (dict "context" $) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "authup.accountConsole.fullname" . }}
  minReplicas: {{ .Values.accountConsole.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.accountConsole.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- if .Values.accountConsole.autoscaling.hpa.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.accountConsole.autoscaling.hpa.targetCPU }}
    {{- end }}
    {{- if .Values.accountConsole.autoscaling.hpa.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.accountConsole.autoscaling.hpa.targetMemory }}
    {{- end }}
{{- end }}
