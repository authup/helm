# Combined server with the optional admin and account surfaces disabled. The
# auth pages remain available because they own the login flow.
postgresql:
  persistence:
    enabled: false
adminConsole:
  enabled: false
accountConsole:
  enabled: false
server:
  resources:
    requests:
      cpu: 50m
      memory: 256Mi
